class Population {
    

    constructor(mutateRate, size) {
        this.numGenerations; // number of Generations;
        this.population = [];

        this.mutateRate = mutateRate;
        this.size = size;
    }
}